# Copy ke .env lalu isi credential dari provider proxy kamu
# Jangan commit .env ke git

# Format saat pakai ./run.sh (paste langsung):
# login,password,host,port
# login:password@host:port

# Atau manual lewat env:
# PROXY_URL=http://login:password@host:port
